auto-install-peers=true

# Allow these native deps to run their build scripts on install (pnpm v9+
# refuses to do this by default). Without this, sharp / esbuild can't compile
# their native binaries on a fresh CI runner.
only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
